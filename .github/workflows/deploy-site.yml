name: Deploy Landing Page

on:
  push:
    branches: [main]
    paths:
      - 'site/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Cubbit S3
        uses: ./
        with:
          source-dir: ./site
          bucket: pages
          access-key: ${{ secrets.CUBBIT_ACCESS_KEY }}
          secret-key: ${{ secrets.CUBBIT_SECRET_KEY }}
